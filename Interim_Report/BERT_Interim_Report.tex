\documentclass[12pt]{project_report}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath,amsthm,graphicx}
\usepackage{hyperref} % For links
\usepackage{float} % For image placement

\raggedbottom% Avoid stretch behavior

\title{Low-Cost FPGA-Based\\Bit Error Rate Test set}
\author{Vishnu Barath}
\date{\today}
\supervisor{Martyn Fice}
%\advisor{My advisor}
\aiuse{}
\wordcount{2497}

\begin{document}

\coverpage% Cover page
\declaration% Declaration
\maketitle

\abstract{
    This project aims to build a low-cost system that can test the Bit Error Ratio of a communication channel.
    It will use Pseudorandom Binary Sequence generators to generate a deterministic bitstream which will be transmitted across the channel. 
    The sequence will be generated on both the transmitter and the receiver to compare and calculate the BER.\@
    So far, a transmitter has been developed which generates a repeating PRBS-7 sequence with a length of 127 bits.
    The PRBS generator is implemented using a Linear Feedback Shift Register controlled by a 10MHz clock where a bit is transferred at every rising edge, achieving a bitrate of 10Mbps.
    This has been implemented on an FPGA.\@
}

\tableofcontents

\clearpage
\section{Project Description}
    Communication networks are continuously expanding in capacity and speed and being able to ensure signal integrity is paramount.
    The most common example of such a system is the internet, which uses undersea optical fibres for data transmission.
    Such networks have become a central component today with applications in many industries, such as telecommunications, cloud services, banking, healthcare and transportation.
    In accordance with these advancements, techniques to measure  performance of communication channels have evolved.
    Such channels are susceptible to incorrect transmissions due factors such as noise or interference.
    A metric of performance for communication channels is the Bit Error Ratio (BER) which can be characterised via a Bit Error Rate Test (BERT).~\cite{SMJAlam2011}

    The BER of a channel is the proportion of erroneous bits in a transmission.
    When received data has errors it requires retransmission or extra processing from error correction.
    Hence, high BERs would significantly impact bitrate capability.~\cite{SMJAlam2011}
    Being able to measure this is important in ensuring speed and efficiency in data transmission.
    Unfortunately, systems that measure BERs can be expensive and would be designed for bitrates in the Gbps range.
    Acquiring such equipment could prove difficult and excessive since high speeds aren't always required.~\cite{MJBasford2020}
    
    This project aims to construct a system that can measure the BER of a Visible Light Communication (VLC) channel at bitrates up to 10Mbps, while keeping costs relatively low. Below are the main goals and objectives. 

    \begin{enumerate}
        \item Generate a Pseudorandom Binary Sequence (PRBS)
        \begin{itemize}
            \item To test capability of the hardware choice
            \item To set a fixed bit sequence length
            \item To generate an apparently random, but known, bit sequence
            \item To reach bitrates of up to 10Mbps
        \end{itemize}

        \item Develop an Error detection algorithm
        \begin{itemize}
            \item To receive bitstream at up to 10Mbps
            \item To generate PRBS on receiver side
            \item To synchronise PRBS with the received bitstream
            \item To calculate BER
        \end{itemize}
    \end{enumerate}

\clearpage
\section{Literature Review}
    Building a system to measure a BER can be done via  programmable devices such as Microcontrollers or Field Programmable Gate Arrays (FPGA).
    A sequence of known bits is required for testing a BER.\@
    One common method for generating one is the PRBS.\@
    Such sequences are generated using a Linear Feedback Shift Register (LFSR) where two specific bits in the register are inputs to an XOR gate then into the Least Significant Bit of the register.~\cite{DRiccardi2011,MGeorge2007}
    Low cost BERTs have been developed using such methods.~\cite{MJBasford2020, SChatterjee2023, NMarda2014}
    Examples of these are discussed below.

    \subsection{Serial Communication BERT}
        An FPGA-based device was developed to test the BER of a serial communication channel.
        It uses a 31-bit LFSR to generate a PRBS which would have a length of \begin{math}
            2^{31} - 1
        \end{math} bits ({$\sim$}\begin{math}
            2.14 \times 10^9
        \end{math}).~\cite{MJBasford2020,DRiccardi2011}

        The system works by transmitting a PRBS to an external channel and receiving the bitstream on the same FPGA.\@
        The bits are generated, received and compared simultaneously.
        It's capable of testing bitrates of up to 50Mbps and used the Anritsu MP8302A BERT for reference.~\cite{MJBasford2020}
        By transmitting and receiving on the same FPGA, having to synchronise bitstreams wasn't an issue.
        However, this isn't good for simulating realistic transmission between two different devices.
    
    \subsection{VLC BERT}
        A PCB with logic circuits was designed to test a VLC channel, the same type of channel this project is meant for.
        In this case, the bit generator is a square wave rather than a PRBS.\@
        Effectively, it is a sequence of alternating ones and zeros.~\cite{SChatterjee2023}

        Two separate PCBs transmit and receive the bitstreams.
        This BERT can test for bitrates up to 370kbps, which is significantly lower than this project's target.
        Using a periodic square wave simplifies the issue of synchronising the transmitted and received bitstreams.
        This system is significantly cheaper and less complex than FPGA equivalents.
        Unfortunately, bitstreams realistically won't be sequential ones and zeros, meaning the displayed BER may not reflect real world conditions for data transmission.~\cite{SChatterjee2023}
    
    \subsection{Microcontroller-based BERT}
        An STM32 Microcontroller combined with a Complex Programmable Logic Device was used to make a BERT.\@
        A specific type of communication channel isn't specified.
        This system uses pseudorandom patterns to test a channel.~\cite{NMarda2014}

        A single STM32 transmits and receives the bitstreams and tests the BER at\\2.048Mbps.~\cite{NMarda2014}
        This bitrate is similar to what this project is targetting.
        Due to using only one device, this may not accurately simulate real world conditions.

    \subsection{Summary}
        Overall, both FPGAs and Microcontrollers are valid options in building a BERT since they can both be programmed to transmit and receive a PRBS and compare bitstreams.
        To best simulate realistic transmission, separate devices for transmitting and receiving would be ideal.
        While this may increase cost, relative to the price of BER equipment, it shouldn't be significant.~\cite{MJBasford2020,SChatterjee2023}
        A PRBS generator will be used to generate bits for calculating the BER.\@
        \autoref{fig:BERT_Block} gives an overview of the system.

        \begin{figure}[H]
            \centering
            \includegraphics[width=1\textwidth]{img/BERT_Block_Diagram.png}
            \caption{Block Diagram of BERT system}\label{fig:BERT_Block}
        \end{figure}
        
\clearpage
\section{Work Performed to-date}

    \subsection{Hardware Choice}
        In this project, there is a choice between Microcontrollers or FPGAs.
        To determine which is suitable, the capabilities of a device of each were tested using a 10MHz clock since this would allow bitrates of 10Mbps (bits are transferred on each rising edge).

        \subsubsection{MC}
            The board being tested here is the NUCLEO-F401RE which has STM32F401RET6 Microcontroller using the Serial Peripheral Interface (SPI) communication protocol and programmed using STM32CUBEIDE.~\cite{NUCLEO2014,STM2014}

            \begin{figure}[H]
                \centering
                \includegraphics[width=1\textwidth]{img/MC/10MHz_Clock.png}
                \caption{10MHz clock from NUCLEO-F401RE (Oscilloscope capture)}\label{fig:10MHz_MC}
            \end{figure}

            In \autoref{fig:10MHz_MC}, the clock is the expected frequency and peak-to-peak voltage 
            (\begin{math}
                V_{pp}
            \end{math}).
            However, the signal varies exponentially rather than instantaneously like a square wave.
            Different frequency clocks should be tested for this effect.

            \begin{figure}[H]
                \centering
                \includegraphics[width=1\textwidth]{img/MC/500kHz_Clock.png}
                \caption{500kHz clock from NUCLEO-F401RE (Oscilloscope capture)}\label{fig:500kHz_MC}
            \end{figure}

            \begin{figure}[H]
                \centering
                \includegraphics[width=1\textwidth]{img/MC/42MHz_Clock.png}
                \caption{42MHz clock from NUCLEO-F401RE (Oscilloscope capture)}\label{fig:42MHz_MC}
            \end{figure}

            \autoref{fig:500kHz_MC} shows the microcontroller producing a square wave clock at 500kHz, suggesting a measurement equipment limitation.
            In \autoref{fig:42MHz_MC}, the microcontroller is producing a clock at its maximum frequency of 42MHz.~\cite{NUCLEO2014,STM2014}
            The signal is almost triangular, but still holds a fundamental frequency of 42MHz.
            However, 
            \begin{math}
                V_{pp}
            \end{math}
            is attenuated compared to that at 10MHz in \autoref{fig:10MHz_MC} and 500kHz in \autoref{fig:500kHz_MC}.

            The RT-ZPO3 oscilloscope probe being used has a -3dB bandwidth of 10MHz which explains the distortion in the 10MHz and 42MHz clocks.~\cite{R&S2019}
            The Fourier Series of a square wave, with its fundamental frequency, has higher frequencies that allow the wave to be square.~\cite{JASvoboda2013}
            So in a 10MHz square wave, there are higher frequencies which allow for the wave to be square.
            These frequencies are attenuated, giving the output in \autoref{fig:10MHz_MC}.
            The 10MHz frequency is within the bandwidth, so there's little attenuation in
            \begin{math}
                V_{pp}
            \end{math},
            unlike at 42MHz in \autoref{fig:42MHz_MC}.
            From testing at a lower frequency, it can be concluded that the hardware still produces a square wave at 10MHz with measurements being limited by the probe.

        \subsubsection{FPGA}
            The board being tested here is a DE0-Nano with an ALTERA Cyclone IV EP4CE22-\\F17C6 FPGA programmed in Quartus Prime Lite.~\cite{Terasic2012}

            \begin{figure}[H]
                \centering
                \includegraphics[width=1\textwidth]{img/FPGA/10MHz_Clock.png}
                \caption{10MHz clock from DE0-Nano (Oscilloscope capture)}\label{fig:FPGA_10MHz}
            \end{figure}

            \autoref{fig:FPGA_10MHz} shows that at a 10MHz clock, the FPGA provides a similar output to the microcontroller, with the probe causing similar distortion.

            \begin{figure}[H]
                \centering
                \includegraphics[width=1\textwidth]{img/FPGA/500kHz_Clock.png}
                \caption{500kHz clock from DE0-Nano (Oscilloscope capture)}\label{fig:FPGA_500kHz}
            \end{figure}

            \begin{figure}[H]
                \centering
                \includegraphics[width=1\textwidth]{img/FPGA/50MHz_Clock.png}
                \caption{50MHz clock from DE0-Nano (Oscilloscope capture)}\label{fig:FPGA_50MHz}
            \end{figure}

            When the clock is turned down to 500kHz, in \autoref{fig:FPGA_500kHz}, like the microcontroller, the wave appears square due to the clock frequency being within the bandwidth of the probe.~\cite{R&S2019}

            The FPGA allows for a maximum clock frequency of 50MHz.~\cite{Terasic2012}
            The clock signal in \autoref{fig:FPGA_50MHz} faces similar distortion issues to the 42MHz clock from the microcontroller but at a greater degree due to being a higher frequency.
            Since none of these effects occur at lower frequencies, this appears to be a limitation of the probe being used, not the board.

        \subsubsection{Final Choice}
            Both hardware choices display similar results in testing with clock signals meaning they are both capable of being used to develop a BERT.\@
            The FPGA has a higher maximum clock frequency (50MHz) over the microcontroller (42MHz) suggesting it can be pushed further for higher bitrates.~\cite{NUCLEO2014,STM2014,Terasic2012}

            Microcontrollers are programmed at a higher level than FPGAs, meaning they are easier to work with.
            However, this provides minimal control over the lower level logic.
            FPGAs allow for a higher degree of control by working directly at the logic circuit level, but this makes them difficult to program.

            Microcontrollers have interfaces for transmitting data synchronised to a clock e.g. I2C, SPI.\@
            But these peripherals use processes which may require more than one clock cycle per transferred bit.
            The FPGA being used has an ADC but no other peripherals since it works at the logic circuit level.
            Despite being difficult to program, FPGAs can be programmed in a way that allows one bit to be transferred per clock cycle.~\cite{STM2014,Terasic2012}
            For direct control registers' status, a low level of programming is required.
            Microcontrollers are generally cheaper than FPGAs, and low cost is one of the aims of this project.
            However, compared to the price of BER equipment, this difference in price is small, meaning despite the hardware choice, the costs will most likely be relatively low.~\cite{MJBasford2020,SChatterjee2023}
            
            In this project, an LFSR would generate the PRBS, meaning direct control over logic is necessary.
            The clock rates available are in the Megahertz range rather than Gigahertz, so using clock cycles efficiently is necessary.
            Considering these factors, the FPGA has been chosen to develop the BERT.\@
    
    \subsection{PRBS generator}
        For this project, a PRBS generator will be used to generate a bitstream for the BERT.\@
        At this point in time, a PRBS-7 generator has been made.
        It repeats every 127 bits and is represented by the following polynomial.~\cite{DRiccardi2011}
        \begin{equation}
            x^7 + x^6 + 1
        \end{equation}
        This polynomial isn't mathematical, it's a representation of an LFSR that generates a PRBS-7.
        The values of the powers represent which bits are used for feedback.
        This polynomial says that the 7th and 6th bits of the register are inputs to an XOR gate where the output feeds into the Least Significant Bit of the LFSR.\@
        The Most Significant Bit of the LFSR is transmitted to the communication channel every clock cycle.~\cite{DRiccardi2011,MGeorge2007}
        This can be implemented using D-Type Flip-Flops in Quartus Prime.
        Typically, larger LFSRs would be used for PRBS generation to achieve a higher degree of randomness, as demonstrated in the Serial Communication BERT mentioned previously.~\cite{MJBasford2020}

        \subsubsection{Push Button Clock}
            For testing purposes, the clock will be controlled by a push button on the DE0-Nano with the status of the LFSR on LEDs.
            Refer to \autoref{fig:FPGA_CLK_BTN} in the appendices for the logic circuit.
            The initial status of the LFSR is set to 7 ones. 
            The buttons on the FPGA are active low meaning they become logic 0 when pressed and logic 1 when released.
            The LFSR status changes when the button is released, not when pressed down, meaning the PRBS generator is synchronised to the clock's rising edge which is expected.
            Once the button is pressed 127 times, the LFSR's status returns to the original state of 7 ones.
            Hence, PRBS generator produces a repetitive bit sequence of length 127, which is also expected.
            When the initial seed is 7 ones, the expected repeating 127 bit sequence is the following.\\ 111111100000010000011000010100011110010001011001110101001111101000011100\\0100100110110101101111011000110100101110111001100101010\\
            This can be compared to the output of the PRBS generator viewed on an oscilloscope.

        \subsubsection{500kHz Clock}
            The 50MHz oscillator on the FPGA is initiated and connected to a Phase Locked Loop (PLL) to divide it by 100, producing a 500kHz clock.
            This clock controls the PRBS generator.
            Refer to \autoref{fig:FPGA_CLK_500k} in the appendices for the logic circuit.

            \begin{figure}[H]
                \centering
                \includegraphics[width=1\textwidth]{img/FPGA/PRBS_500kHz_1.png}
                \caption{Oscilloscope capture of PRBS generator at 500kHz (first 60 bits)}\label{fig:FPGA_PRBS_500K_1}
            \end{figure}

            \begin{figure}[H]
                \centering
                \includegraphics[width=1\textwidth]{img/FPGA/PRBS_500kHz_2.png}
                \caption{Oscilloscope capture of PRBS generator at 500kHz (61\textsuperscript{st}-120\textsuperscript{th} bits)}\label{fig:FPGA_PRBS_500K_2}
            \end{figure}

            \begin{figure}[H]
                \centering
                \includegraphics[width=1\textwidth]{img/FPGA/PRBS_500kHz_3.png}
                \caption{Oscilloscope capture of PRBS generator at 500kHz. First 7 bits in the figure are the 121\textsuperscript{st}-127\textsuperscript{th} bits of the sequence. Sequence repeats after}\label{fig:FPGA_PRBS_500K_3}
            \end{figure}
            In \autoref{fig:FPGA_PRBS_500K_1}, \autoref{fig:FPGA_PRBS_500K_2} and \autoref{fig:FPGA_PRBS_500K_3} channel 1 (CH1) is the 500kHz clock and channel 2 (CH2) is the PRBS generator output.
            At 500kHz, the probe doesn't distort the square waveforms.
            When comparing the output to the expected bit sequence, no mistakes are found and the sequence repeats every 127 bits as expected.

        \subsubsection{10MHz Clock}
            The 50MHz oscillator on the FPGA is initiated and connected to a PLL to divide it by 5, producing a 10MHz clock.
            This clock controls the PRBS generator.
            Refer to \autoref{fig:FPGA_CLK_10M} in the appendices for the logic circuit.

            \begin{figure}[H]
                \centering
                \includegraphics[width=1\textwidth]{img/FPGA/PRBS_10MHz_1.png}
                \caption{Oscilloscope capture of PRBS generator at 10MHz (first 60 bits)}\label{fig:FPGA_PRBS_10M_1}
            \end{figure}

            \begin{figure}[H]
                \centering
                \includegraphics[width=1\textwidth]{img/FPGA/PRBS_10MHz_2.png}
                \caption{Oscilloscope capture of PRBS generator at 10MHz (61\textsuperscript{st}-120\textsuperscript{th} bits)}\label{fig:FPGA_PRBS_10M_2}
            \end{figure}

            \begin{figure}[H]
                \centering
                \includegraphics[width=1\textwidth]{img/FPGA/PRBS_10MHz_3.png}
                \caption{Oscilloscope capture of PRBS generator at 10MHz. First 7 bits in the figure are the 121\textsuperscript{st}-127\textsuperscript{th} bits of the sequence. Sequence repeats after}\label{fig:FPGA_PRBS_10M_3}
            \end{figure}

            In \autoref{fig:FPGA_PRBS_10M_1}, \autoref{fig:FPGA_PRBS_10M_2} and \autoref{fig:FPGA_PRBS_10M_3}, CH1 is the 10MHz clock and CH2 is the PRBS generator output.
            At 10MHz, the probe distorts the square waveform due to high frequency components being attenuated.
            The probe used has a -3dB bandwidth of 10MHz.~\cite{R&S2019,JASvoboda2013}
            No mistakes are found when comparing the output to the expected bit sequence and the sequence repeats every 127 bits as expected.

        \subsubsection{20MHz Clock}
            The PLL to multiply the 50MHz oscillator by 2/5, producing a 20MHz clock.
            This clock controls the PRBS generator.
            Refer to \autoref{fig:FPGA_CLK_20M} in the appendices for the logic circuit.

            \begin{figure}[H]
                \centering
                \includegraphics[width=1\textwidth]{img/FPGA/PRBS_20MHz_1.png}
                \caption{Oscilloscope capture of PRBS generator at 20MHz (first 120 bits)}\label{fig:FPGA_PRBS_20M_1}
            \end{figure}

            \begin{figure}[H]
                \centering
                \includegraphics[width=1\textwidth]{img/FPGA/PRBS_20MHz_2.png}
                \caption{Oscilloscope capture of PRBS generator at 20MHz First 7 bits in the figure are the 121\textsuperscript{st}-127\textsuperscript{th} bits of the sequence. Sequence repeats after}\label{fig:FPGA_PRBS_20M_2}
            \end{figure}

            In \autoref{fig:FPGA_PRBS_20M_1} and \autoref{fig:FPGA_PRBS_20M_2}, CH1 is the 20MHz clock and CH2 is the PRBS generator output.
            At 20MHz, the probe distorts the square wave due to high frequency components being attenuated.
            This happens at a greater degree than at 10MHz.~\cite{JASvoboda2013}
            The bit sequence produced at 20MHz is the same as the expected bit sequence.
                        
\section{Project Management}
    \begin{figure}[H]
        \centering
        \includegraphics[width=1\textwidth]{img/Gantt_Chart.png}
        \caption{Original Gantt Chart}\label{fig:Gantt_Chart_1}
    \end{figure}
    So far, the tasks have been completed on schedule with the risk assessment and hardware choice being completed quicker than expected. There was an overlap in time in hardware decision and PRBS development since generating a clock signal is a part of it. 

    \begin{figure}[H]
        \centering
        \includegraphics[width=1\textwidth]{img/Gantt_Chart_2.png}
        \caption{Modified Gantt Chart for Term 2}\label{fig:Gantt_Chart_2}
    \end{figure}

    The tasks for Term 2 have been broken down further.
    Algorithm development for decision regions isn't a priority and will be disregarded if reasonable progress isn't made.
    Bitstream synchronisation must be completed first since it's a required condition for calculating the BER.\@
    This part may be the most difficult hence it has the most allocated time.

\section{Future Work}
    For the decision regions, a voltage threshold will be used where above would be considered logic 1 and below would be considered logic 0. This would require using an ADC to read voltages.
    In bitstream synchronisation, a method of aligning the beginning of the received and generated PRBS needs to be developed.
    For BER calculations, a fixed number of bits would need to be set for which the received and generated bitstreams are compared for to display the BER as a value.

\bibliographystyle{IEEEtran} % IEEE style
\bibliography{references}

\appendix
\section{Appendices}
\subsection{Quartus Prime Lite Block Diagrams}

    \begin{figure}[H]
        \centering
        \includegraphics[width=1\textwidth]{img/FPGA/PRBS_CLK_BTN.png}
        \caption{PRBS Generator with the clock tied to push button}\label{fig:FPGA_CLK_BTN}

    \end{figure}

    \begin{figure}[H]
        \centering
        \includegraphics[width=1\textwidth]{img/FPGA/PRBS_CLK_500kHz.png}
        \caption{PRBS Generator implemented with 500kHz clock}\label{fig:FPGA_CLK_500k}
    \end{figure}

    \begin{figure}[H]
        \centering
        \includegraphics[width=1\textwidth]{img/FPGA/PRBS_CLK_10MHz.png}
        \caption{PRBS Generator implemented with 10MHz clock}\label{fig:FPGA_CLK_10M}
    \end{figure}

    \begin{figure}[H]
        \centering
        \includegraphics[width=1\textwidth]{img/FPGA/PRBS_CLK_20MHz.png}
        \caption{PRBS Generator implemented with 20MHz clock}\label{fig:FPGA_CLK_20M}
    \end{figure}

\end{document}
