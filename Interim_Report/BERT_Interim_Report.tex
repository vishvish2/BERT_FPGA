\documentclass[12pt]{project_report}
\usepackage{graphicx} % Required for inserting images
% \usepackage[margin=1in]{geometry} % 1 inch margins
\usepackage{amsmath} % Equations
\usepackage{hyperref} % For links
\usepackage{float} % For image placement
\usepackage{breqn}
\usepackage{tikz}

\title{Low-Cost FPGA-Based\\(Bit Error Rate Test set)}
\author{Vishnu Barath}
\date{\today}
\supervisor{Martyn Fice}
%\advisor{My advisor}
\aiuse{}
\wordcount{2}

\begin{document}

\coverpage\
\declaration\
\maketitle

% \begin{center}
%     \vspace{3cm}
%     {\large \textbf{3\textsuperscript{rd} Year Project Interim Report}}\\[0.7cm]
%     {\large Department of Electronic \&\\ Electrical Engineering}\\[0.5cm]
%     {\large UCL}\\[0.7cm]
%     {\large Supervisor: Dr Martyn Fice}\\[1.5cm]
%     \date{\today}
% \end{center}
% \clearpage
% I have read and understood UCL's and the Department's statements and guidelines concerning plagiarism.\\[0.5cm]
% I declare that all material described in this report is my own work except where explicitly and individually indicated in the text.
% This includes ideas described in the text, figures and computer programs.\\[0.5cm]
% This report contains \_ pages (excluding this page and the appendices) and \_ words.\\[0.5cm]
% % TODO Fill these in at the end
% Signed: Vishnu Barath \hfill Date: \date{\today}

\clearpage
\begin{abstract}
    This project aims to build a low-cost system that can test the Bit Error Ratio (BER) of a communication channel.
    It will use Pseudorandom Binary Sequence (PRBS) generators to generate a known bitstream which will be transmitted across the channel. 
    The sequence will be generated on both the transmitter and the receiver to compare and calculate the BER.\@
    So far, a transmitter has been successfully developed which generates a repeating PRBS-7 sequence with a length of 127 bits.
    The PRBS generator is implemented using a Linear Feedback Shift Register controlled by a 10MHz clock where a bit is transferred at every rising edge, achieving a bitrate of 10Mbps.
    This has been implemented on an FPGA.\@
\end{abstract}

\tableofcontents

\clearpage
\section{Project Description}
    Communication networks are continuously expanding in capacity and speed and being able to ensure signal integrity is paramount.
    The most common example of such a system is the internet, which uses undersea optical fibres for data transmission.
    Such networks have become a central component in the modern world with applications in many important industries, such as telecommunications, cloud services, banking, healthcare and transportation.
    In accordance with these advancements, techniques to measure the performance of communication channels have evolved.
    Such channels are susceptible to incorrect transmissions arising due factors such as noise or interference.
    A metric of performance for communication channels is the Bit Error Ratio (BER) which can be characterised via a Bit Error Rate Test (BERT).~\cite{SMJAlam2011}

    The BER of a channel is the proportion of bits in a transmission which have an error.
    When received data has errors it requires retransmission or extra processing from error correction.
    Hence, high BERs would significantly impact bitrate capability.~\cite{SMJAlam2011}
    Being able to measure this is important in ensuring speed and efficiency in data transmission.
    Unfortunately, systems that measure BERs can be expensive and would be designed for bitrates in the Gbps range.
    As a result, acquiring such equipment could prove difficult and excessive since such high speeds aren't always required.~\cite{MJBasford2020}
    
    This project aims to construct a system that can accurately measure the BER of a Visible Light Communication (VLC) channel at bitrates up to 10Mbps, while keeping costs relatively low. Below are the main goals and objectives of this project. 

    \begin{enumerate}
        \item Make a Pseudo Random Binary Sequence (PRBS) generator
        \begin{itemize}
            \item To test the capability of the hardware choice
            \item To set a fixed bit sequence length
            \item To generate a known sequence of bits that appear random
            \item To reach a bitrate of up to 10Mbps
        \end{itemize}

        \item Develop an Error detection algorithm
        \begin{itemize}
            \item To receive bitstream at up to 10Mbps
            \item To generate the PRBS on receiver side
            \item To synchronise the PRBS with the received bitstream
            \item To calculate an BER
        \end{itemize}
    \end{enumerate}

\clearpage
\section{Literature Review}
    Building a system to measure a BER can be done via  programmable devices such as Microcontrollers or Field Programmable Gate Arrays (FPGA).
    A sequence of known bits is required for testing a BER.\@
    One common method for generating one is the PRBS.\@
    Such sequences are generated using a Linear Feedback Shift Register (LFSR) where two specific bits in the register are fed into an XOR gate then into the Least Significant Bit (LSB) of the register.~\cite{DRiccardi2011,MGeorge2007}
    Low cost BERTs have been developed using such methods.~\cite{MJBasford2020, SChatterjee2023, NMarda2014}
    Examples of these are discussed below.

    \subsection{Serial Communication BER Tester}
        An FPGA-based device was developed to test the BER of a serial communication channel.
        It uses a 31-bit LSFR to generate a PRBS which would have a length of \begin{math}
            2^{31} - 1
        \end{math} bits ({$\sim$}\begin{math}
            2.14 \times 10^9
        \end{math}).~\cite{MJBasford2020,DRiccardi2011}

        The system works by transmitting a PRBS to an external channel and receiving the bitstream on the same FPGA.\@
        The bits are generated, received and compared simultaneously.
        It is capable of testing bitrates of up to 50Mbps and used the Anritsu MP8302A BERT for reference.~\cite{MJBasford2020}
        By transmitting and receiving on the same FPGA, having to synchronise bitstreams wasn't an issue.
        However, this isn't good for simulating realistic transmission between two different devices.
    
    \subsection{VLC BER Tester}
        A PCB with logic circuits was designed to test a VLC channel, the same type of communication channel this project is meant for.
        In this case, the bit generator is a square wave rather than a PRBS.\@
        Effectively, the bit sequence being used to test the BERT is a sequence of alternating ones and zeros.~\cite{SChatterjee2023}

        Two separate PCBs are used for transmitting and receiving the bitstreams.
        This BERT can test for bitrates up to 370kbps, which is significantly lower than this project's target.
        Using a periodic square wave simplifies the issue of synchronising the transmitted and received bitstreams.
        This system is significantly cheaper and less complex than FPGA equivalents.
        Unfortunately, bitstreams realistically won't be sequential ones and zeros, meaning the displayed error ratio may not necessarily reflect real world conditions for data transmission.~\cite{SChatterjee2023}
    
    \subsection{Microcontroller-based BER}
        An STM32 Microcontroller combined with a Complex Programmable Logic Device (CPLD) was used to make a BER tester.
        A specific type of communication channel isn't specified.
        This system uses pseudorandom patterns to test a channel.~\cite{NMarda2014}

        A single STM32 is used for transmitting and receiving the bitstreams and tests the error ratio at 2.048Mbps.~\cite{NMarda2014}
        This bitrate is similar to what this project is targetting.
        Due to using only one device, this may not accurately simulate real world conditions.

    \subsection{Summary}
        Overall, both FPGAs and Microcontrollers are valid options in building a BER tester since they can both be programmed to transmit and receive a PRBS and compare bitstreams.
        In order to best simulate realistic transmission, two separate devices for transmitting and receiving would be ideal.
        While this may increase cost, relative to the price of BER equipment, it shouldn't be significant.~\cite{MJBasford2020,SChatterjee2023}
        A PRBS generator will be used to generate a bitstream that will be used for calculating the BER.\@
        \autoref{fig:BERT_Block} gives an overview of the system.

        \begin{figure}[H]
            \centering
            \includegraphics[width=0.8\textwidth]{img/BERT_Block_Diagram.png}
            \caption{Block Diagram of BER Testing system}\label{fig:BERT_Block}
        \end{figure}
        
\clearpage
\section{Work Performed to-date}

    \subsection{Choice of Hardware}
        In this project, there is choice between using Microcontrollers or FPGAs.
        To determine which is suitable, the capabilities of a device of each were tested using a 10MHz clock. If a bitstream is synchronised to this clock, each bit would be transferred on the rising edge of the clock, allowing for a bitrate of 10Mbps.

        \subsubsection{MC}
            The board being tested here is the NUCLEO-F401RE board which has an STM32F401RET6 Microcontroller using the Serial Peripheral Interface (SPI) synchronous serial communication protocol.~\cite{NUCLEO2014,STM2014}
            It was programmed using STM32CUBEIDE.\@

            \begin{figure}[H]
                \centering
                \includegraphics[width=0.8\textwidth]{img/MC/10MHz_Clock.png}
                \caption{Oscilloscope capture of 10MHz clock from NUCLEO-F401RE}\label{fig:10MHz_MC}
            \end{figure}

            In \autoref{fig:10MHz_MC}, the clock is the correct frequency with an adequate peak-to-peak voltage.
            However, the signal rises and falls exponentially rather than instantaneously like a typical square wave would.
            Different frequency clocks need to be tested for this effect.

            \begin{figure}[H]
                \centering
                \includegraphics[width=0.8\textwidth]{img/MC/500kHz_Clock.png}
                \caption{Oscilloscope capture of 500kHz clock from NUCLEO-F401RE}\label{fig:500kHz_MC}
            \end{figure}

            \autoref{fig:500kHz_MC}, shows the microcontroller producing a square wave clock at 500kHz, suggesting that there is a limitation in the measurement equipment. 

            \begin{figure}[H]
                \centering
                \includegraphics[width=0.8\textwidth]{img/MC/42MHz_Clock.png}
                \caption{Oscilloscope capture of 42MHz clock from NUCLEO-F401RE}\label{fig:42MHz_MC}
            \end{figure}

            In \autoref{fig:42MHz_MC}, the microcontroller is producing a clock at its highest possible frequency of 42MHz.~\cite{NUCLEO2014,STM2014}
            The signal is almost triangular, but still holds a fundamental frequency of 42MHz.
            However, the peak-to-peak voltage (2.14V) is attenuated compared to that at 10MHz (3.36V) in \autoref{fig:10MHz_MC} and 500kHz (3.48V) in \autoref{fig:500kHz_MC}.

            The oscilloscope probe being used (RT-ZPO3) has a -3dB bandwidth of 10MHz which explains the distortion in the 10MHz and 42MHz clocks.~\cite{R&S2019}
            This is because the Fourier Series of a square wave, along with its fundamental frequency, has other higher frequencies that allow the wave to be square.~\cite{JASvoboda2013}
            So even in a 10MHz square wave, there are high frequencies  which allow for the wave to be square.
            These frequencies are attenuated, giving the output in \autoref{fig:10MHz_MC}.
            But since the 10MHz frequency is within the bandwidth, there is little attenuation in the peak-to-peak voltage, unlike at 42MHz in \autoref{fig:42MHz_MC}.
            From testing at a lower frequency, it can be concluded that the hardware still produces a square wave at 10MHz with measurements being limited by the probe.

        \subsubsection{FPGA}
            The board being tested here is a DE0-Nano with an ALTERA Cyclone IV EP4CE22F17C6 FPGA.\@
            It was programmed via Quartus Prime Lite.~\cite{Terasic2012}

            \begin{figure}[H]
                \centering
                \includegraphics[width=0.8\textwidth]{img/FPGA/10MHz_Clock.png}
                \caption{Oscilloscope capture of 10MHz clock from the DE0-Nano}\label{fig:FPGA_10MHz}
            \end{figure}

            \autoref{fig:FPGA_10MHz} shows that at a 10MHz clock, the FPGA provides a similar output to the microcontroller, with the probe causing the same type of distortion.
            The peak-to-peak voltages are similar.

            \begin{figure}[H]
                \centering
                \includegraphics[width=0.8\textwidth]{img/FPGA/500kHz_Clock.png}
                \caption{Oscilloscope capture of 500kHz clock from the DE0-Nano}\label{fig:FPGA_500kHz}
            \end{figure}

            When the clock is turned down to 500kHz, in \autoref{fig:FPGA_500kHz}, like the microcontroller, the wave appears as a square wave due to the clock frequency being within the bandwidth of the oscilloscope probe.~\cite{R&S2019}

            \begin{figure}[H]
                \centering
                \includegraphics[width=0.8\textwidth]{img/FPGA/50MHz_Clock.png}
                \caption{Oscilloscope capture of 50MHz clock from the DE0-Nano}\label{fig:FPGA_50MHz}
            \end{figure}

            The FPGA allows for a maximum clock frequency of 50MHz.~\cite{Terasic2012}
            The clock signal in \autoref{fig:FPGA_50MHz} faces similar issues to the 42MHz clock from the microcontroller (distortion and attenuation) but at a greater degree due to being a higher frequency.
            Since none of these effects occur at lower frequencies, this appears to be a limitation of the probe being used, not the board.

        \subsubsection{Final Choice}
            Both hardware choices display similar results in testing with clock signals meaning they are both capable of being used to develop a BERT.\@
            The FPGA has a higher maximum clock frequency of 50MHz over the microcontroller's 42MHz suggesting it can be pushed further to test higher bitrates.~\cite{NUCLEO2014,STM2014,Terasic2012}

            Microcontrollers are programmed in higher level programming languages than FPGAs, meaning they are easier to work with.
            However, this provides minimal control over the lower level logic.
            FPGAs allow for a higher degree of control by working directly at the logic circuit level, but this makes them harder to program.

            Microcontrollers also have peripherals you can work with, including UART, ADCs and interfaces such as I2C and SPI.\@
            These peripherals are designed for transferring bitstreams synchronised to a clock signal.
            On the other hand, these peripherals have complex workings which may require more than one clock cycle per transferred bit.
            The FPGA being used has an ADC but no other peripherals since it works directly at the logic circuit level.
            Despite being difficult to program, logic circuits can be designed in a way that allows for one bit to be transferred per clock cycle.~\cite{STM2014,Terasic2012}
            When working with registers, a low level of programming is required for direct control.
            Microcontrollers tend to be cheaper than FPGAs, and low cost is one of the aims of this project.
            However, compared to the price of BER equipment, this difference in price is small, meaning despite the choice of hardware, the costs will most likely be relatively low.~\cite{MJBasford2020,SChatterjee2023}
            
            In this project, an LSFR would be required for PRBS generation, meaning direct control over logic is necessary.
            The clock rates available are in the Megahertz range rather than Gigahertz, so it would be ideal to make efficient use of clock cycles.
            Considering these factors, the FPGA has been chosen to develop the BERT.\@
    
    \subsection{PRBS generator}
        For this project, a PRBS generator will be used to generate a bitstream for the BERT.\@
        At this point in time, a PRBS-7 generator has been made.
        It repeats every 127 bits and is represented by the following polynomial.~\cite{DRiccardi2011}
        \begin{equation}
            x^7 + x^6 + 1
        \end{equation}
        This polynomial isn't mathematical, it is a representation of the LFSR that would be used to generate a PRBS-7.
        The values of the powers represent which bits are used for feedback.
        This polynomial says that the 7th and 6th bits of the register are inputs to an XOR gate where the output is fed into the Least Significant Bit (LSB) of the shift register.
        The Most Significant Bit (MSB) of the LFSR is transmitted to the communication channel.~\cite{DRiccardi2011,MGeorge2007}
        This can be implemented using D-Type Flip-Flops in Quartus Prime.
        Typically, larger LSFRs would be used to generate a PRBS in order to achieve a higher degree of randomness, as demonstrated in the Serial Communication BER Tester mentioned previously.~\cite{MJBasford2020}
        \subsubsection{Push Button Clock}

            For testing purposes, the clock will be controlled by a push button on the DE0-Nano with the status of the LFSR on LEDs.
            Refer to \autoref{fig:FPGA_CLK_BTN} in the appendices for the logic circuit.
            The initial status of the LFSR is set to 7 ones. 
            The buttons on the FPGA are active low meaning they become logic level 0 when pressed and logic level 1 when released.
            The LSFR status changes when the button is released, not when pressed down.
            This means the PRBS generator is synchronised to the rising edge of the clock which is expected behaviour.
            Once the button is pressed 127 times, the LSFR's status returns to the original state of 7 ones.
            Hence, PRBS generator is producing a repetitive bit sequence of length 127, which is also expected.

            When the initial seed is 7 ones, the expected repeating 127 bit sequence is the following.
            \begin{flushleft}
                \texttt{1111111000000100000110000101000111100100010110011101010011111010000111000100} \\
                \texttt{100110110101101111011000110100101110111001100101010}
            \end{flushleft}
            This can be compared to the output of the PRBS generator viewed on an oscilloscope.

        \subsubsection{500kHz Clock}

            The 50MHz oscillator on the FPGA is initiated and connected to a Phase Locked Loop (PLL) to divide it by 100, producing a 500kHz clock.
            This signal is fed into the clock input of the PRBS generator.
            Refer to \autoref{fig:FPGA_CLK_500k} in the appendices for the logic circuit.

            \begin{figure}[H]
                \centering
                \includegraphics[width=0.9\textwidth]{img/FPGA/PRBS_500kHz_1.png}
                \caption{Oscilloscope output of PRBS generator at 500kHz (first 60 bits)}\label{fig:FPGA_PRBS_500K_1}
            \end{figure}

            \begin{figure}[H]
                \centering
                \includegraphics[width=0.9\textwidth]{img/FPGA/PRBS_500kHz_2.png}
                \caption{Oscilloscope output of PRBS generator at 500kHz (61\textsuperscript{st}-120\textsuperscript{th} bits)}\label{fig:FPGA_PRBS_500K_2}
            \end{figure}

            \begin{figure}[H]
                \centering
                \includegraphics[width=1\textwidth]{img/FPGA/PRBS_500kHz_3.png}
                \caption{Oscilloscope output of PRBS generator at 500kHz. First 7 bits in the figure are the 121\textsuperscript{st}-127\textsuperscript{th} bits of the sequence. Sequence repeats after}\label{fig:FPGA_PRBS_500K_3}
            \end{figure}
            In \autoref{fig:FPGA_PRBS_500K_1}, \autoref{fig:FPGA_PRBS_500K_2} and \autoref{fig:FPGA_PRBS_500K_3} channel 1 (CH1) is the 500kHz clock and channel 2 (CH2) is the PRBS generator output.
            At 500kHz, the oscilloscope probes don't distort the square waveforms.
            When comparing the output to the expected bit sequence, no mistakes are found and the sequence repeats every 127 bits as expected.

        \subsubsection{10MHz Clock}

            The 50MHz oscillator on the FPGA is initiated and connected to a PLL to divide it by 5, producing a 10MHz clock.
            This signal is fed into the clock input of the PRBS generator.
            Refer to \autoref{fig:FPGA_CLK_10M} in the appendices for the logic circuit.

            \begin{figure}[H]
                \centering
                \includegraphics[width=0.9\textwidth]{img/FPGA/PRBS_10MHz_1.png}
                \caption{Oscilloscope output of PRBS generator at 10MHz (first 60 bits)}\label{fig:FPGA_PRBS_10M_1}
            \end{figure}

            \begin{figure}[H]
                \centering
                \includegraphics[width=0.9\textwidth]{img/FPGA/PRBS_10MHz_2.png}
                \caption{Oscilloscope output of PRBS generator at 10MHz (61\textsuperscript{st}-120\textsuperscript{th} bits)}\label{fig:FPGA_PRBS_10M_2}
            \end{figure}

            \begin{figure}[H]
                \centering
                \includegraphics[width=0.9\textwidth]{img/FPGA/PRBS_10MHz_3.png}
                \caption{Oscilloscope output of PRBS generator at 10MHz. First 7 bits in the figure are the 121\textsuperscript{st}-127\textsuperscript{th} bits of the sequence. Sequence repeats after}\label{fig:FPGA_PRBS_10M_3}
            \end{figure}

            In \autoref{fig:FPGA_PRBS_10M_1}, \autoref{fig:FPGA_PRBS_10M_2} and \autoref{fig:FPGA_PRBS_10M_3}, CH1 is the 10MHz clock and CH2 is the PRBS generator output.
            At 10MHz, the oscilloscope probes distort the square waveforms due to high frequency components of the square wave being attenuated.
            The probe used has a -3dB bandwidth of 10MHz.~\cite{R&S2019,JASvoboda2013}
            No mistakes are found when comparing the output to the expected bit sequence and the sequence repeats every 127 bits as expected.

        \subsubsection{20MHz Clock}

            The PLL to multiply the 50MHz oscillator by 2/5, producing a 20MHz clock.
            This signal is fed into the clock input of the PRBS generator.
            Refer to \autoref{fig:FPGA_CLK_20M} in the appendices for the logic circuit.

            \begin{figure}[H]
                \centering
                \includegraphics[width=0.9\textwidth]{img/FPGA/PRBS_20MHz_1.png}
                \caption{Oscilloscope output of PRBS generator at 20MHz (first 120 bits)}\label{fig:FPGA_PRBS_20M_1}
            \end{figure}

            \begin{figure}[H]
                \centering
                \includegraphics[width=0.9\textwidth]{img/FPGA/PRBS_20MHz_2.png}
                \caption{Oscilloscope output of PRBS generator at 20MHz First 7 bits in the figure are the 121\textsuperscript{st}-127\textsuperscript{th} bits of the sequence. Sequence repeats after}\label{fig:FPGA_PRBS_20M_2}
            \end{figure}

            In \autoref{fig:FPGA_PRBS_20M_1} and \autoref{fig:FPGA_PRBS_20M_2}, CH1 is the 20MHz clock and CH2 is the PRBS generator output.
            At 20MHz, the oscilloscope probes distort the square waveforms due to high frequency components of the square wave being attenuated.
            This happens at a greater degree than at 10MHz.~\cite{JASvoboda2013}
            The bit sequence produced at 20MHz is the same as the expected bit sequence.
                        
\section{Project Management}
    \begin{figure}[H]
        \centering
        \includegraphics[width=1\textwidth]{img/Gantt_Chart.png}
        \caption{Original Gantt Chart}\label{fig:Gantt_Chart_1}
    \end{figure}
    The project has mostly gone to plan with tasks like the risk assessment and hardware choice being completed quicker than expected. So far, the tasks have been completed on schedule. There was an overlap in time in hardware decision and PRBS development since generating a clock signal is a part of it. 

    \begin{figure}[H]
        \centering
        \includegraphics[width=1\textwidth]{img/Gantt_Chart_2.png}
        \caption{Modified Gantt Chart for objectives in Term 2}\label{fig:Gantt_Chart_2}
    \end{figure}

    The tasks for Term 2 have been broken down further.
    Algorithm development for decision regions isn't a priority and will be disregarded if reasonable progress isn't made.
    Bitstream synchronisation must be completed first since it is a required condition for calculating the error ratio.
    These parts will be the most difficult hence the most time has been allocated to them.

\section{Future Work}
    For the decision regions, a simple voltage threshold will be used where anything above would be considered logic 1 and below would be considered logic 0. This would require using an ADC to read a voltage level.
    In bitstream synchronisation, a method of aligning the beginning of the received and generated PRBS needs to be developed.
    For error rate calculations, a fixed number of bits would need to be set for which the received and generated bitstreams are compared for to display an error rate as a number.
%TODO Include summary/conclusion section (this project is about FPGA based BERT, i've demonstrated a PRBS gen at 2^7 -1)

\bibliographystyle{IEEEtran} % IEEE style
\bibliography{references}

\appendix
\section{Appendices}
\subsection{Quartus Prime Lite Block Diagrams}

    \begin{figure}[H]
        \centering
        \includegraphics[width=1\textwidth]{img/FPGA/PRBS_CLK_BTN.png}
        \caption{PRBS Generator on Quartus Prime Lite with the clock tied to a push button}\label{fig:FPGA_CLK_BTN}

    \end{figure}

    \begin{figure}[H]
        \centering
        \includegraphics[width=1\textwidth]{img/FPGA/PRBS_CLK_500kHz.png}
        \caption{PRBS Generator implemented on Quartus Prime Lite with a 500kHz clock}\label{fig:FPGA_CLK_500k}
    \end{figure}

    \begin{figure}[H]
        \centering
        \includegraphics[width=1\textwidth]{img/FPGA/PRBS_CLK_10MHz.png}
        \caption{PRBS Generator implemented on Quartus Prime Lite with a 10MHz clock}\label{fig:FPGA_CLK_10M}
    \end{figure}

    \begin{figure}[H]
        \centering
        \includegraphics[width=0.9\textwidth]{img/FPGA/PRBS_CLK_20MHz.png}
        \caption{PRBS Generator implemented on Quartus Prime Lite with a 20MHz clock}\label{fig:FPGA_CLK_20M}
    \end{figure}

\end{document}
